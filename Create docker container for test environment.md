---
database: Oracle
title: Backup and Recovery
created: 05/29/2024 15:05
---
```sh
$ORACLE_HOME/bin/rman target / nocatalog > /tmp/rman.out << EOF
show all;
exit;
EOF
```

```sh
cat /tmp/rman.out | grep -v '# default' | grep CONFIGURE
```

```
CONFIGURE DEFAULT DEVICE TYPE TO 'SBT_TAPE';
CONFIGURE DEVICE TYPE 'SBT_TAPE' PARALLELISM 2 BACKUP TYPE TO COMPRESSED BACKUPSET;
CONFIGURE CHANNEL DEVICE TYPE 'SBT_TAPE' PARMS  'SBT_LIBRARY=/u01/app/oracle/product/19.3/dbhome_1/lib/libopc.so, SBT_PARMS=(OPC_PFILE=/u01/app/oracle/product/19.3/dbhome_1/dbs/opcORCL.ora)';
CONFIGURE ENCRYPTION FOR DATABASE ON;
CONFIGURE ENCRYPTION ALGORITHM 'AES128';
CONFIGURE COMPRESSION ALGORITHM 'BASIC' AS OF RELEASE 'DEFAULT' OPTIMIZE FOR LOAD TRUE;
```

```sh
rman target / <<EOF
CONFIGURE DEFAULT DEVICE TYPE CLEAR;
CONFIGURE DEVICE TYPE 'SBT_TAPE' CLEAR;
CONFIGURE CHANNEL DEVICE TYPE 'SBT_TAPE' CLEAR;
CONFIGURE ENCRYPTION FOR DATABASE CLEAR;
CONFIGURE ENCRYPTION ALGORITHM CLEAR;
CONFIGURE COMPRESSION ALGORITHM CLEAR;
exit;
EOF
```

Check

```sh
$ORACLE_HOME/bin/rman target / nocatalog > /tmp/rman.out << EOF
show all;
exit;
EOF

cat /tmp/rman.out | grep -v '# default' | grep CONFIGURE
```
